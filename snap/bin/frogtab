#!/bin/sh

mkdir -p "$SNAP_USER_COMMON"
cd "$SNAP_USER_COMMON"
if [ ! -e config.ini ]; then
  cp "$SNAP/config.ini" .
fi
export FROGTAB_FLASK_SCRIPT="$SNAP/src/frogtab_flask.py"
python "$SNAP/src/frogtab.py" "$@"